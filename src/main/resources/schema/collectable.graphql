"""
Represents a collectable type, which can be a specific category of collectable items.
excludes: ArtBooks, Cards, Sealed Cards, Shoes, and Video Games.
"""
type CollectableType {
    id: ID!
    name: String!
    deleted: Boolean!
    createdAt: String!
    updatedAt: String!
    deletedAt: String
}

input CollectableTypeInput {
    id: ID
    name: String!
}

input CollectableTypeFilter {
    id: ID
    name: String
    deleted: Boolean
}

"""
Represents a collectable item, which cannot be represented by more defined collectable types.
"""
type Collectable {
    id: ID!
    baseCollectable: BaseCollectable!
    type: CollectableType!
    brand: Brand!
    series: Series
    skuId: String!
    deleted: Boolean!
    createdAt: String!
    updatedAt: String!
    deletedAt: String
}

input CollectableInput {
    id: ID
    baseCollectable: BaseCollectableInput!
    typeId: ID!
    brandId: ID!
    seriesId: ID
    skuId: String!
}

input CollectableFilter {
    id: ID
    baseCollectable: BaseCollectableFilter
    typeId: ID
    brandId: ID
    seriesId: ID
    skuId: String
    deleted: Boolean
}
